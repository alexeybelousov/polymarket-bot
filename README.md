# Polymarket Trading Bot 🤖

Telegram бот для отслеживания торговых сигналов на Polymarket.

## Функционал

- **Сигналы "3 свечи"** - отслеживание паттерна 3 свечей одного цвета на 15-минутных рынках ETH и BTC
- **Персональные настройки** - каждый пользователь может включить/выключить нужные сигналы
- **Реальное время** - проверка каждые 10 секунд

## Установка

### 1. Клонирование и установка зависимостей

```bash
pnpm install
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/polymarket-bot
```

### 3. Запуск MongoDB

```bash
# Локально
mongod

# Или через Docker
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### 4. Запуск бота

```bash
# Production
pnpm start

# Development (с auto-reload)
pnpm dev
```

## Структура проекта

```
src/
├── index.js              # Точка входа
├── config.js             # Конфигурация
├── bot/
│   ├── index.js          # Создание Telegram бота
│   ├── handlers.js       # Обработчики команд и кнопок
│   └── keyboards.js      # Inline клавиатуры
├── models/
│   └── User.js           # MongoDB модель пользователя
└── services/
    ├── polymarket.js     # Работа с Polymarket Gamma API
    └── signalTracker.js  # Отслеживание сигналов
```

## Логика сигнала "3 свечи"

Сигнал отправляется когда:

1. ✅ 2 предыдущие закрытые 15-минутные свечи одного цвета (обе зелёные или обе красные)
2. ✅ Текущая активная свеча того же цвета более 10 секунд
3. ✅ До закрытия рынка осталось минимум 1 минута

## API

Бот использует [Polymarket Gamma API](https://gamma-api.polymarket.com) для получения данных о рынках:

- ETH: `eth-updown-15m-<timestamp>`
- BTC: `btc-updown-15m-<timestamp>`

## Команды бота

- `/start` - Главное меню с кнопками управления

## TODO (Trading)

В будущем планируется добавить:
- [ ] Автоматический трейдинг на Polymarket
- [ ] Управление позициями
- [ ] Статистика сделок

