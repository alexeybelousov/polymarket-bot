services:
  - type: worker
    name: polymarket-bot
    runtime: node
    buildCommand: pnpm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: DEBUG
        value: false

